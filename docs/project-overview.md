# 프로젝트 개요

[위키 홈](README.md)

WebLog는 대용량 웹 서버 로그를 파싱해 프로젝트별 DB에 저장하고 조회하는 데스크톱 도구다.

## 확정 범위

- Rust + Tauri 2 + React/TypeScript + 프로젝트별 영속 DuckDB.
- Windows x64 가상 인스턴스, RAM 16GB, 증설 가능한 HDD. 압축 후 100GB 이상 입력을 고려한다.
- Apache/Nginx 접근 로그, IIS W3C, 커스텀 단일행 텍스트. 일반 파일과 `.gz` 지원.
- 서버 선택 → 경로 → 재귀/파일명 탐색 → 파일별 포맷 검사·편집 → 파싱 → DB 저장 → 조회.
- 기본 프리셋, 퍼즐/YAML 편집, 테이블과 구조화 트리, 재구성 로그 제공.
- 원문은 저장하지 않는다. 파싱 필드와 출처 파일·줄 위치를 저장한다. 실패 원문도 복제하지 않는다.
- 재구성 로그는 원본과 바이트 단위로 같지 않으며 정확한 원문으로 표시하지 않는다.
- 실시간 감시, 멀티라인 에러 로그, 다중 사용자 접속은 최초 범위에서 제외한다.

## 구현 및 검증 상태

최초 범위와 현재 구현 상태를 구분한다. 단계별 진행 상태와 남은 작업은 [구현 순서](implementation-plan.md), 시나리오별 실행 여부는 [검증 기록](verification.md), 성능 측정 결과는 [벤치마크](benchmarks.md)에서 관리한다.

설치·실행 방법과 현재 화면 동작은 [사용 안내](../README.md)를 참고한다. 단일행 에러 로그 등 이후 추가된 동작도 사용 안내에서 확인한다.

## 관련 설계

- [아키텍처와 데이터 처리](architecture.md): 계층 경계, 자원 상한, 저장·복구 제약.
- [하네스 설계](harness-design.md): 상세 처리 흐름과 대용량 설계 근거.
- [데이터 모델](data-model.md): 출처, 작업·배치 상태, 결과 버전.
- [파서 포맷](parser-format.md): 프리셋과 포맷 정의.
