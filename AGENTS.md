# WebLog 공통 개발 규칙

## 작업 방식
- 작업 전 [프로젝트 위키](docs/README.md)에서 관련 문서를 확인한다. 구현 시 [프로젝트 범위](docs/project-overview.md), [아키텍처](docs/architecture.md), [설계](docs/harness-design.md)와 관련 명세를 따르고 [구현 순서](docs/implementation-plan.md)를 확인한다.
- 합의한 범위는 반복 승인 없이 진행한다. 기술 스택과 데이터 보존 정책을 변경하려면 이유와 영향을 제안한다.
- 관련 없는 리팩터링을 피하고, 의존성은 필요 이유를 기록한다. 사용자 변경을 덮어쓰지 않는다.
- 새 스킬 설치·업데이트 및 하위 에이전트 사용은 사용자 요청이 있을 때 수행한다.
- 설명은 한국어, 식별자는 영어. 완료 시 변경 내용·검사 결과·미검증 사항을 보고한다.
- 코드 주석은 영어로 작성하고, 필요한 의미가 전달되는 범위에서 최대한 짧게 쓴다.

## PLAN 관리

- 사용자 요청을 받으면 작업에 착수하기 전에 PLAN을 세우고, 저장소 루트 기준 `agent/plan/plan-<작업 관련 계획 이름>-<yymmdd>.md`에 저장한다. 계획 이름은 작업을 식별할 수 있는 영어 소문자·하이픈 조합으로, 날짜는 작성일로 정한다.
- PLAN은 Markdown Todo 체크박스(`- [ ]`) 목록으로 작성하고, 작업 중 진행 상황을 해당 파일에 갱신한다.
- 기능 추가·변경·버그 수정의 PLAN에는 `테스트 코드 작성 → 기능 구현 → 해당 테스트 실행·결과 확인`을 순서대로 별도 체크박스 항목으로 넣는다. 계획 단계에서 검증할 동작·예상 결과·테스트 실행 명령을 정하고, 기능 구현보다 테스트 코드를 먼저 작성한다.
- 사용자 요청 중 위키에 남길 내용이 있으면 테스트 실행·결과 확인 다음에 `위키 반영` 항목을 추가한다. 완료 근거에는 갱신한 문서 경로와 반영 내용을 기록한다.
- 계획 항목을 완료하면 `- [x]`로 체크하고 바로 다음 줄에 들여쓴 `근거:`를 작성한다. 변경 파일·위치, 실행한 명령과 결과 등 완료를 확인할 수 있는 구체적인 근거를 남긴다.
- 미완료·미검증 항목은 체크하지 않는다. 계획이 변경되면 항목과 변경 이유를 함께 갱신한다.

```markdown
- [ ] 요청 동작과 예상 결과를 검증하는 테스트 코드를 먼저 작성한다.
- [ ] 테스트가 요구하는 기능을 구현한다.
- [ ] 작성한 테스트를 실행하고 예상 동작과 통과 여부를 확인한다.
- [ ] 사용자 요청 중 위키 등재 대상 내용을 관련 문서에 반영한다(대상이 있는 경우).
```

각 항목 완료 시 체크하고 바로 다음 줄에 `근거:`를 적는다. 테스트 작성 근거에는 테스트 파일·검증 시나리오를, 구현 근거에는 변경 파일·동작을, 테스트 실행 근거에는 실제 명령·결과를 기록한다.

## 스킬 사용
아래 파일은 프로젝트에 설치되어 있다. 도구의 자동 발견 여부와 무관하게 관련 작업에서 해당 SKILL.md를 읽고 필요한 상대 참조를 따라간다.
- Rust: `.agents/skills/rust-best-practices/SKILL.md`
- 비동기/취소: `.agents/skills/rust-async-patterns/SKILL.md`
- 테스트: `.agents/skills/rust-testing/SKILL.md`
- Tauri: `.agents/skills/tauri-v2/SKILL.md`
- 새 스킬 검색: `.agents/skills/find-skills/SKILL.md`
- UI 가이드라인 검토: `.agents/skills/web-design-guidelines/SKILL.md`
- 프런트엔드 디자인: `.agents/skills/frontend-design/SKILL.md`
- UI 시각 디자인: `.agents/skills/design-taste-frontend/SKILL.md`
- 최소 구현·YAGNI: `.agents/skills/ponytail/SKILL.md`

스킬 적용 시 개발 지침:
- 제품 코드에서 unwrap/expect로 입력·I/O 오류를 종료시키지 않는다. Result와 명시적 오류를 사용한다.
- Tauri 스킬의 모든 로직을 lib.rs에 둔다는 지침 대신 앱 조립·명령 등록과 핵심 로직을 별도 모듈로 분리한다.
- IPC 구조체의 필드 이름은 serde 규약으로 명시한다. 중첩 필드 자동 변환을 가정하지 않는다.
- 기능 추가·변경·버그 수정은 PLAN의 테스트 우선 순서를 따른다. 단순 문서·스타일 수정에 불필요한 테스트를 추가하지 않으며, 이 경우 PLAN에 예외 사유와 대신 수행할 검사를 기록한다.
- 커버리지 80~100%를 일괄 강제하지 않는다. 데이터 무결성과 실패 시나리오 검증을 우선한다.
- 스킬의 예제 버전·API는 도입 시 공식 문서와 실제 빌드로 검증한다. 스킬 원문은 수정하지 않는다.

## 문서 관리

- AGENTS.md에는 공통 작업 방식·개발 지침·스킬 사용 규칙을 둔다.
- 프로젝트 범위·구조·데이터 정책·구현 현황·검사 명령은 `docs/`의 Markdown 위키에서 관리한다. 새 문서는 [위키 목차](docs/README.md)에 연결한다.
- 사용자 요청 중 지속적으로 참고할 기능 동작·사용법·제약·설계 결정·운영 방법 등은 기능 구현 완료 후 테스트 결과를 확인한 뒤 위키에 등재한다. 기존 관련 문서를 우선 갱신하고, 새 주제일 때 문서를 추가한다. 실제 구현된 내용과 남은 미검증 사항을 구분해 기록한다.
- 기능이나 설계를 변경하면 해당 명세를 함께 갱신하고, 중요한 기술 결정은 `docs/decisions/`에 이유와 대안을 기록한다.
- 같은 내용을 여러 문서에 복제하기보다 담당 문서를 링크한다. 구현 상태와 검증 결과를 구분하고, 과거 기록을 현재 상태로 표현하지 않는다.

## 검증

- 기능 구현이 끝나면 먼저 작성한 테스트를 실제로 실행해 요구 동작을 검증하고 통과 여부를 확인한다. 실패하면 원인을 수정하고 다시 실행한다. 테스트를 작성한 것만으로 검증 완료로 처리하지 않는다.
- 테스트 통과 후에만 PLAN의 해당 검증 항목을 체크한다. 실행할 수 없는 경우 체크하지 않고 사유와 미검증 범위를 근거에 기록한다.
- 변경 범위에 맞는 검사를 실행한다. 명령과 프로젝트별 필수 시나리오는 [검증 기준](docs/testing.md)을 따른다.
- 로컬/CI 공통 검사 진입점을 유지하고 실행법을 README에 문서화한다. 지원 feature 조합만 검사한다.
- 실행하지 않은 대용량 검사나 Windows 검사를 통과했다고 보고하지 않는다.
